# 🚀 部署指南

## 📋 依赖包清单

已生成 `requirements.txt` 文件，包含以下依赖：

### 核心框架
- **streamlit** >= 1.37.0 - Web 应用框架
- **pandas** >= 2.3.0 - 数据处理
- **numpy** >= 1.23.0 - 数值计算

### 金融数据
- **yfinance** >= 0.2.0 - Yahoo Finance API

### 数据可视化
- **matplotlib** >= 3.5.0 - 图表绘制

### 网络和解析
- **requests** >= 2.27.0 - HTTP 请求
- **beautifulsoup4** >= 4.10.0 - HTML 解析

### 文本分析
- **textblob** >= 0.17.0 - 情感分析

### 用户认证
- **streamlit-authenticator** >= 0.4.0 - 认证组件
- **bcrypt** >= 5.0.0 - 密码哈希
- **cryptography** >= 46.0.0 - 加密支持
- **PyJWT** >= 2.11.0 - JWT 令牌
- **PyYAML** >= 6.0.0 - YAML 配置

### 额外组件
- **extra-streamlit-components** >= 0.1.81 - 额外 UI 组件
- **captcha** >= 0.7.0 - 验证码
- **python-dateutil** >= 2.8.0 - 日期处理

## 🔧 安装步骤

### 1. 创建虚拟环境（推荐）
```bash
python -m venv venv
source venv/bin/activate  # Linux/Mac
# 或
venv\Scripts\activate  # Windows
```

### 2. 安装依赖
```bash
pip install -r requirements.txt
```

### 3. 配置环境变量
在 `.streamlit/secrets.toml` 中配置：
```toml
SUPABASE_URL = "your_supabase_url"
SUPABASE_KEY = "your_supabase_key"
```

### 4. 运行应用
```bash
streamlit run StockAnalyser.py
```

## 🌐 部署选项

### Streamlit Cloud
1. 将代码推送到 GitHub
2. 在 Streamlit Cloud 中连接仓库
3. 配置环境变量
4. 自动部署

### Docker 部署
```dockerfile
FROM python:3.9-slim
WORKDIR /app
COPY requirements.txt .
RUN pip install -r requirements.txt
COPY . .
EXPOSE 8501
CMD ["streamlit", "run", "StockAnalyser.py"]
```

### 传统服务器
```bash
# 安装依赖
pip install -r requirements.txt

# 使用 Gunicorn 或 uWSGI 部署
gunicorn -w 4 -k uvicorn.workers.UvicornWorker --bind 0.0.0.0:8501 StockAnalyser:app
```

## 🗄️ 数据库设置

### Supabase 配置
1. 创建 Supabase 项目
2. 执行以下 SQL 创建表：

```sql
-- 用户认证表
CREATE TABLE user_credentials (
    id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    username TEXT UNIQUE NOT NULL,
    email TEXT UNIQUE NOT NULL,
    password_hash TEXT NOT NULL,
    salt TEXT NOT NULL,
    created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW(),
    updated_at TIMESTAMP WITH TIME ZONE DEFAULT NOW(),
    last_login TIMESTAMP WITH TIME ZONE,
    is_active BOOLEAN DEFAULT TRUE
);

CREATE INDEX idx_user_credentials_username ON user_credentials(username);
CREATE INDEX idx_user_credentials_email ON user_credentials(email);

-- 用户持仓表
CREATE TABLE user_portfolio (
    id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    user_email TEXT NOT NULL,
    stock_list TEXT DEFAULT '',
    updated_at TIMESTAMP WITH TIME ZONE DEFAULT NOW(),
    created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW()
);

CREATE INDEX idx_user_portfolio_user_id ON user_portfolio(user_email);
```

## 🔒 安全配置

### 1. 环境变量
- 不要在代码中硬编码敏感信息
- 使用 `.streamlit/secrets.toml` 或环境变量

### 2. 数据库安全
- 启用 Row Level Security (RLS)
- 设置适当的访问策略
- 定期备份

### 3. 应用安全
- 使用 HTTPS
- 定期更新依赖包
- 监控异常访问

## 📊 性能优化

### 1. 缓存
- Streamlit 的 `@st.cache_data` 装饰器
- 数据库查询缓存
- API 响应缓存

### 2. 异步处理
- 长时间运行的任务异步化
- 使用进度条提升用户体验

### 3. 资源管理
- 限制并发用户数
- 监控内存使用
- 优化数据库查询

## 🔍 故障排除

### 常见问题
1. **依赖冲突**: 使用虚拟环境隔离依赖
2. **端口占用**: 修改端口或停止占用进程
3. **数据库连接**: 检查网络和认证信息
4. **内存不足**: 优化数据处理逻辑

### 日志监控
```python
import logging
logging.basicConfig(level=logging.INFO)
```

## 📱 测试

### 本地测试
```bash
# 运行测试脚本
python test_auth_complete.py
python test_workflow.py
```

### 部署测试
1. 功能测试
2. 性能测试
3. 安全测试
4. 用户体验测试

---

**部署完成！** 🎉

应用现在可以安全部署到生产环境，具备完整的用户认证和数据管理功能。
